Installing
==========

To install from source:

./configure
make
make install (as root)

To create and install a RedHat RPM file:

Follow the above steps
make dist
Copy the tarball to /usr/src/rpm/SOURCES
rpmbuild -bb --clean gmameui.spec

